app-id: com.cherry_ai.cherrystudio
runtime: org.freedesktop.Platform
runtime-version: '25.08'
sdk: org.freedesktop.Sdk
base: org.electronjs.Electron2.BaseApp
base-version: '25.08'
command: cherrystudio
finish-args:
  - --socket=wayland
  - --share=network
  - --device=dri
  - --talk-name=org.kde.StatusNotifierWatcher
  - --system-talk-name=org.freedesktop.login1
  - --system-talk-name=org.chromium.bluetooth.Manager
  # required to fix cursor scaling on wayland
  - --env=XCURSOR_PATH=/run/host/user-share/icons:/run/host/share/icons

add-extensions:
  org.freedesktop.Platform.electron:
    version: '25.08'
    directory: electron

cleanup:
  - '*.a'
  - '*.la'
  - /include
  - /lib/cmake
  - /lib/debug
  - /lib/pkgconfig
  - /man
  - /share/man
  - /share/doc
  - /share/gtk-doc

modules:
  - name: cherrystudio
    buildsystem: simple
    build-options:
      no-debuginfo: true
    build-commands:
      - mkdir -pv ${FLATPAK_DEST}/electron
      - chmod -v +x Cherry-Studio.AppImage
      - ./Cherry-Studio.AppImage --appimage-extract
      - mv -v squashfs-root/resources ${FLATPAK_DEST}/cherrystudio
      - desktop-file-edit --set-key="Icon" --set-value="com.cherry_ai.cherrystudio"
        --set-key="Categories" --set-value="Utility;Network;" --set-key="Exec" --set-value="cherrystudio
        %U" --remove-key="X-AppImage-Version" squashfs-root/cherrystudio.desktop
      - install -Dvm644 squashfs-root/cherrystudio.desktop ${FLATPAK_DEST}/share/applications/${FLATPAK_ID}.desktop
      - install -Dvm644 ${FLATPAK_ID}.metainfo.xml -t ${FLATPAK_DEST}/share/metainfo
      - install -Dvm644 ${FLATPAK_ID}.png -t ${FLATPAK_DEST}/share/icons/hicolor/512x512/apps
      - install -Dvm755 cherrystudio.sh ${FLATPAK_DEST}/bin/cherrystudio
    sources:
      - type: file
        only-arches: [x86_64]
        url: https://github.com/CherryHQ/cherry-studio/releases/download/v1.6.1/Cherry-Studio-1.6.1-x86_64.AppImage
        sha256: 950613e9713de1cba19d0c9105d16e11efeb1c4a4545e6cf538902c8f885ecb2
        dest-filename: Cherry-Studio.AppImage
        x-checker-data:
          type: json
          url: https://api.github.com/repos/CherryHQ/cherry-studio/releases/latest
          version-query: .tag_name | sub("^v"; "")
          url-query: .assets[] | select(.name | endswith("x86_64.AppImage")) | .browser_download_url
      - type: file
        only-arches: [aarch64]
        url: https://github.com/CherryHQ/cherry-studio/releases/download/v1.6.1/Cherry-Studio-1.6.1-arm64.AppImage
        sha256: 1ffd153ee65f94ad87869402934890e56e045eec55b750b9892cf3b775575811
        dest-filename: Cherry-Studio.AppImage
        x-checker-data:
          type: json
          url: https://api.github.com/repos/CherryHQ/cherry-studio/releases/latest
          version-query: .tag_name | sub("^v"; "")
          url-query: .assets[] | select(.name | endswith("arm64.AppImage")) | .browser_download_url
      - type: file
        path: com.cherry_ai.cherrystudio.metainfo.xml
      - type: file
        path: com.cherry_ai.cherrystudio.png
      - type: script
        dest-filename: cherrystudio.sh
        commands:
          - export TMPDIR=${XDG_RUNTIME_DIR}/app/${FLATPAK_ID}
          - exec zypak-wrapper "/app/electron/electron" "/app/cherrystudio/app.asar"
            "--enable-features=UseOzonePlatform" "--ozone-platform=wayland" "--enable-wayland-ime"
            "--wayland-text-input-version=3" "$@"
